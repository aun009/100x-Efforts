

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}


model Todos {
  id Int @default(autoincrement()) @id
  title String
  desc String
}